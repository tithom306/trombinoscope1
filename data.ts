
import { Project } from './types';

/**
 * Les données sont maintenant chargées dynamiquement via fetch dans App.tsx
 * pour éviter les erreurs de résolution de modules JSON.
 */
export const EMPTY_PROJECTS: Project[] = [];
